# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Full Width Image Test Suite >> Validate Full Width Image Display
- Location: /Users/gunashekar/Automation/SportsBeach/sportsbeachautomation/playwright/specs/web/demo.test.ts:18:7

# Error details

```
Error: Timed out 5000ms waiting for expect(locator).toBeVisible()

Locator: locator('img.full-width-image')
Expected: visible
Received: <element(s) not found>
Call log:
  - expect.toBeVisible with timeout 5000ms
  - waiting for locator('img.full-width-image')

    at /Users/gunashekar/Automation/SportsBeach/sportsbeachautomation/playwright/specs/web/demo.test.ts:32:32
```

# Test source

```ts
   1 | import { test, expect } from '@playwright/test';
   2 | import { POManager } from '../../pageobjects/POManager';
   3 |
   4 | test.describe('Full Width Image Test Suite', () => {
   5 |
   6 |   test.beforeEach(async ({ page, baseURL }) => {
   7 |     if (typeof baseURL === 'string') {
   8 |       await page.goto(baseURL);
   9 |     } else {
  10 |       throw new Error('baseURL is not defined or not a string');
  11 |     }
  12 |   });
  13 |
  14 |   test.afterEach(async ({ page }) => {
  15 |     await page.close();
  16 |   });
  17 |
  18 |   test('Validate Full Width Image Display', async ({ page }) => {
  19 |
  20 |     // Create objects to access the page objects
  21 |     const poManager = new POManager(page);
  22 |     const basePage = poManager.getBasePage();
  23 |     const schedulePage = poManager.getSchedulePage();
  24 |
  25 |     // Navigate to the schedule page
  26 |     basePage.scheduleLink.click();
  27 |
  28 |     // Step 1: Locate the image element (You may need to adjust the selector based on your actual page structure)
  29 |     const imageLocator = page.locator('img.full-width-image'); // Example class, adjust as needed
  30 |
  31 |     // Step 2: Ensure the image is visible
> 32 |     await expect(imageLocator).toBeVisible(); // Validate the image is present on the page
     |                                ^ Error: Timed out 5000ms waiting for expect(locator).toBeVisible()
  33 |
  34 |     // Step 3: Get the image's width and the viewport width
  35 |     const imageBox = await imageLocator.boundingBox(); // Get the image's bounding box details
  36 |     const viewportSize = await page.viewportSize(); // Get the viewport size of the page
  37 |
  38 |     // Check if either the imageBox or viewportSize is null and throw an error if so
  39 |     if (!imageBox || !viewportSize) {
  40 |       throw new Error('Unable to retrieve image box or viewport size.');
  41 |     }
  42 |
  43 |     // Step 4: Validate that the image spans the full width of the screen
  44 |     expect(imageBox.width).toBeGreaterThanOrEqual(viewportSize.width - 1); // Validate the image width is nearly equal to the viewport width
  45 |
  46 |     // Step 5: Optionally check for other conditions, such as aspect ratio (if applicable)
  47 |     // For example, check if the image height is within an expected range based on the aspect ratio
  48 |     const imageHeight = imageBox.height;
  49 |     const imageWidth = imageBox.width;
  50 |     const expectedAspectRatio = 16 / 9; // Example aspect ratio
  51 |     const aspectRatio = imageWidth / imageHeight;
  52 |
  53 |     expect(Math.abs(aspectRatio - expectedAspectRatio)).toBeLessThan(0.1); // Optional: Assert that aspect ratio is approximately correct
  54 |
  55 |   });
  56 | });
  57 |
```